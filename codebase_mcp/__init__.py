"""Servidor MCP de análisis de codebases (grafo de código + inventario + histórico).

Toda la inteligencia (parser, grafo, inventario, versionado, sincronización) vive en este
paquete; `codebase_server.py` es solo la capa MCP fina por encima.
"""

__version__ = "0.1.0"
