# Dependencias solo para desarrollo y tests (no necesarias en producción).
#   pip install -r requirements-dev.txt
-r requirements.txt
-r requirements-codebase.txt
pytest>=8
pytest-cov>=5
respx>=0.21
